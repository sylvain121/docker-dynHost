# docker-dynHost
dynhost client for ovh dynamic dns

start docker with command line : 

```
sudo docker run -e "LOGIN=mMyLogin" -e "PASSWORD=MySecret" -e "HOSTNAME=test.exemple.ovh" ovh-dynhost /bash
```
